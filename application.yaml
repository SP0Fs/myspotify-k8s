apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: your-spotify
  namespace: argo-cd
spec:
  project: spof-cluster
  source:
    repoURL: 'https://github.com/SP0Fs/myspotify-k8s.git'
    targetRevision: HEAD
    path: resources
  destination:
    server: "https://kubernetes.default.svc"
    namespace: your-spotify
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true